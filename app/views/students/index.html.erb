<!-- STICKY HEADER  -->

<div class="classy-students-header">
   <h1>  Welcome User! </h1>
   <p>Please update your students' information to ensure best results.</p>
</div>
<div class="header" id="student-header">
<div class="classy-sticky-button">
  <div class="d-flex">
    <div class="yellow-btn">View All</div>
    <%= button_to 'Save and sort', sortbutton_path, method: :post, class: "yellow-btn" %>
  </div>
</div>
</div>

<!-- STUDENT CARD  -->
<div class="container m-auto">
  <% @students.each  do |student| %>
  <div class="student-card-container">
    <div class="student-card d-flex  flex-grow-1 justify-content-around">
        <img class="avatar-large" alt="avatar-large" src="<%= student.photo %>" />
        <p><%= student.first_name %></p>
        <div class="student-info">
          <!-- student information container -->
          <div class="student ml-3">

            <p>#<%= student.schoolID %></p>
            <p><%= student.birthday %></p>
            <p><%= student.gender.capitalize %></p>
          </div>
        </div>
        <!-- /student information container -->

        <div class="checkbox-icons">
          <%= simple_form_for student, html: { class: "check_form"}, remote: true do |f| %>
            <%= f.error_notification %>
                <div class="d-flex">
                    <%= f.input :esl, input_html: {id: "student_esl_#{student.id}"}, label: '<i class="fas fa-globe-americas"></i>'.html_safe %>
                    <%= f.input :gifted_talented, input_html: {id: "student_gifted_talented_#{student.id}"}, label: '<i class="fas fa-hat-wizard"></i>'.html_safe %>
                    <%= f.input :special_education, input_html: {id: "student_special_education_#{student.id}"}, label: '<i class="fas fa-star"></i>'.html_safe %>
                    <%= f.input :medical_alert, input_html: {id: "student_medical_alert_#{student.id}"}, label: '<i class="fas fa-plus-square"></i>'.html_safe %>
                  <%= f.submit %>
                </div>

            <%#= f.input :notes %>
        </div>
        <!-- insert checkbox here -->
        <!-- /insert checkbox here -->
          <%= simple_form_for @do_not_place do |f| %>

          <% end %>
          <%= simple_form_for @do_not_place, html: { class: "select_form"}, remote: true do |f| %>

            <div class="do-not-place">
              <%= f.error_notification %>
              <%= f.input :student_one_id, input_html: { value: student.id }, as: :hidden %>
              <%= f.association :student_two, label: false, collection: student.grade.students.map { |student| [student.first_name, student.id] }%>
              <%= f.submit %>
            </div>
          <% end %>
          <!-- <div class="dnp-display border w-100 h-100">
            <%# student.do_not_places.each do |dnp| %>
              <p class="dnp badge badge-danger">
              <%#= dnp.first_name %>
              </p>
            <%# end %>
          </div> -->
          <div id="view-dropdown">
            <i class="fas fa-chevron-down"></i>
          </div>
    </div>
    <div class="dropdown-card">
        <div class="mr-3 ml-4 w-50 h-25">
          <div class="student-content">
          Leave a note
          <%= simple_form_for student, html: { class: "text_form"},  remote: true do |f| %>
            <%= f.error_notification %>
            <%= f.input :notes, input_html: {class: "h-100 w-100"}, label: false, placeholder: "Notes" %>
            <%= f.submit %>
          <% end %>
          </div>
        </div>
        <div class="mr-3 ml-4 w-25">
          <div class="student-content">
            Assign to
            <%= simple_form_for @teacher_lock, html: { class: "select_form"},  remote: true do |f| %>
              <%= f.error_notification %>
              <%= f.input :student_id, input_html: { value: student.id }, as: :hidden %>
              <%= f.association :teacher,
                    label: false,
                    collection: Teacher.all.map {|teacher| [teacher.teacher_name, teacher.id]} %>
              <%= f.submit %>
            <% end %>
          </div>
        </div>

      </div>
    </div>


    <% end %>
<% end  %>
  </div>
</div>


<!-- /STUDENT CARD  -->
<%= link_to "home", "/", class: "btn btn-primary" %>
<br>  <br>

